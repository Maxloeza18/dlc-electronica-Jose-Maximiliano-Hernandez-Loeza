## Semana 1 - Entrada 1 (Lunes)

**Prompt:** 
"Ayúdame a estructurar la semana 1 y explica la práctica del lunes con Python idiomático."

**Resultado:**
- Se organizó la estructura del repositorio para semana 1.
- Se configuró el entorno virtual en Linux.
- Se resolvieron errores de imports, entorno y ejecución.
- Se implementó un ejemplo funcional con `Enum`, `dataclass`, `Protocol` y funciones puras.
- Se validó el proyecto con `mypy` y `ruff`.


## Semana 1 - Entrada 2 (Martes)

**Prompt:**
 "Analiza la actividad del dia martes, dame el plan de desarrollo y dame la explicacion detallada de las funciones que utilizare"

**Resultado:**
- Se implementó la FSM del semáforo orientada a objetos.
- Se organizó la estructura de pruebas separadas.
- Se explicó cómo importar y ejecutar cada archivo.
- Se dejó listo el trabajo para validación y commit.

## Semana 1 - Entrada 3 (Miercoles)

**Prompt:** 
"desarrollemos esta actividad en conjunto (se anexo archivo con la descripcion)" 

**Resultado:**
- Se estableció la estructura para el Día 3 dentro del repositorio[cite: 4].
- Se estructuro la implementación del dominio de sensores (`solid_srp_ocp_lsp.py`), aplicando los principios SRP, OCP y LSP con ejemplos explícitos de mal y buen diseño.

**Prompt:** "requiero que propongas una nueva actividad que siga el mismo principio de la que ya realizamos" 

Resultado:
- Se propuso un nuevo escenario de desarrollo basado en un Sistema de Hogar Inteligente (Smart Home).
- Se acordó agrupar todo el trabajo validado en un solo commit al final de la sesión.

## Semana 1 - Entrada 4 (Jueves)

**Prompt:** 
"estructura la actividad del dia de hoy para que la desarrollemos juntos, explica cada cosa tecnica"

**Resultado:**
- Se analizó la necesidad de aplicar los principios Interface Segregation (ISP) y Dependency Inversion (DIP).
- Se diseñó la estructura de los archivos para la subcarpeta especificada.
- Se explicaron los detalles técnicos detrás de separar interfaces gruesas y cómo inyectar dependencias utilizando `Protocol` en Python para habilitar el testing en memoria.

## Semana 1 - Entrada 5 (Viernes)

**Prompt:** "estructura la actividad del dia viernes, explica cada concepto y aplicacion tecnica"

**Resultado:**
- Se esquematizó el proyecto "El Driver Modernizado".
- Se explicaron los conceptos de inmutabilidad (`dataclass`), interfaces (`abc`) e inyección de dependencias (DIP) aplicados a cada archivo específico del driver.
- Se definieron los parámetros para interactuar con la IA de forma iterativa, escribiendo primero las firmas de los métodos y docstrings.